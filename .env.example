DATABASE_URI=postgresql://postgres:change-me@localhost:5432/madhav-gas
AUTH_SECRET=replace-with-at-least-32-random-characters
NEXT_PUBLIC_SITE_URL=http://localhost:3000
ADMIN_EMAIL=admin@mbga.in
ADMIN_PASSWORD=change-this-before-production

# Server-only SMTP configuration. Never expose these as NEXT_PUBLIC_* values.
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=smtp-user@example.com
SMTP_PASSWORD=replace-with-provider-password
SMTP_FROM_EMAIL=no-reply@example.com
SMTP_FROM_NAME=Madhav Bharat Gas Agency
ADMIN_NOTIFICATION_EMAIL=operations@example.com
